# Optional API token for authentication.
# When set, API requests must include the header:
#   x-trace-lens-token: <your-token>
# or
#   Authorization: Bearer <your-token>
TRACE_LENS_API_TOKEN=your-secret-token-here
